# This file is installed by the `lyra-usb-gadget` package.
# To modify its behavior, use a file in `/etc/udev/rules.d`.

# rename the gadget interface to something unique
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="configfs-gadget.lyra", KERNEL=="usb*", NAME="lyragadget0"
